apiVersion: v1
kind: Service
metadata:
  name: {{ include "warthunder-air-compare.fullname" . }}
  labels:
    {{- include "warthunder-air-compare.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "warthunder-air-compare.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
